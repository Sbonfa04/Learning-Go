#include <stdio.h>

int main() {
    int *p; //p is a pointer to an integer
    
    struct punto { //punto is a struct
        int x; 
        int y; 
    };
    struct punto p1; //p1 is a struct of type punto

    int a[10]; //a is an array of 10 integers
}